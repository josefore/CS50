// Archivo de prueba para las linked lists

#include <stdio.h>
#include <stdlib.h>
#include <cs50.h>

int casilla[3][649];
int a, b, c;
int ctr = 0;

void ordenar(void);

int main(void)
{
    casilla[0][0] = 0;
    casilla[1][0] = 0;
    casilla[2][0] = -1;
}

int algoritmo(void)
{
    // Estilo clasico
    while (ctr < 649)
    {
        for (int i = 0; i < )
    }

    // Ultimo caso
    if (a == 0 && b == 0 && c == 0)
    {

    }
}